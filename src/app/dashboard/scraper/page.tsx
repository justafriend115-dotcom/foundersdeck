import { ScraperWidget } from "@/components/dashboard/scraper-widget";

export const metadata = { title: "Startup Data Scraper" };

export default function ScraperPage() {
  return <ScraperWidget />;
}